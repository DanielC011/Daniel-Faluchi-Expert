import React from 'react';

/**
 * Floating / Sticky WhatsApp was removed per user request.
 * All CTAs are now naturally positioned in the flow of the landing page.
 */
export const FloatingWhatsApp: React.FC = () => {
  return null;
};

